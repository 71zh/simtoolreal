defaults:
  - SimToolRealLSTMAsymmetricPPO
  - _self_

params:
  config:
    name: ${resolve_default:SimToolRealKnifePhase1LSTMAsymmetricPPO,${....experiment}}
    entropy_coef: 0.005
    minibatch_size: 65536

    central_value_config:
      minibatch_size: 65536
